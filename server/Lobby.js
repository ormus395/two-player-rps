class Lobby {
  constructor(room) {
    this.clients = {};
    this.room = room;
    this.game;
  }

  init() {}

  // add users to lobby

  // sends the lobby data
  // users and game state
  // to the lobby
  sendDataToClient() {}
}
